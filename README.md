I built an image to create a container for the website tempalte from tooplate.com "mini_finance"

command to use 
# docker build -t "image name":version .
# docker run -d --name "contanier name" -p 8080:80 "image_name":version 
